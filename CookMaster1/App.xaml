<Application x:Class="CookMaster1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>

        <!-- =========================
             GLOBAL PALETTE & BRUSHES
             ========================= -->
        <Color x:Key="PrimaryColor">#2B6CB0</Color>
        <Color x:Key="PrimaryDark">#234E7A</Color>
        <Color x:Key="AccentColor">#F6AD55</Color>
        <Color x:Key="Surface">#FFFFFF</Color>
        <Color x:Key="MutedText">#6B7280</Color>
        <Color x:Key="Background">#F3F4F6</Color>

        <SolidColorBrush x:Key="AppBackground" Color="{StaticResource Background}" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDark}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource Surface}" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedText}" />

        <!-- =========================
             TYPOGRAPHY & COMMON STYLES
             ========================= -->
        <Style x:Key="HeadingText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}" />
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="Margin" Value="0,6,0,2" />
        </Style>

        <!-- =========================
             INPUT FIELD STYLES
             ========================= -->
        <Style TargetType="TextBox" x:Key="AppTextBox">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="PasswordBox" x:Key="AppPasswordBox" BasedOn="{StaticResource AppTextBox}">
            <Setter Property="Padding" Value="8,6" />
        </Style>

        <!-- =========================
             BUTTON STYLES
             ========================= -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Width" Value="120" />
            <Setter Property="Margin" Value="6,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              Margin="12,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
        </Style>

        <!-- =========================
             CARD STYLE (for centered dialogs/forms)
             ========================= -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="24" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#222" BlurRadius="8" ShadowDepth="2" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- =========================
             OPTIONAL: Global Window style (rounded chrome)
             NOTE: This changes the Window chrome; if you prefer the standard OS titlebar,
             skip applying this global style and style individual windows instead.
             Below is a safe, simple global Window style that keeps the system titlebar.
             ========================= -->
        <Style TargetType="Window" x:Key="AppWindowStyle">
            <Setter Property="Background" Value="{StaticResource AppBackground}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <!-- Make this style active app-wide by adding a default Style without x:Key -->
        <Style TargetType="Window" BasedOn="{StaticResource AppWindowStyle}" />

    </Application.Resources>
</Application>
